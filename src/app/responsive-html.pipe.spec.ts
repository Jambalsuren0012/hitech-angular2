import { ResponsiveHtmlPipe } from './responsive-html.pipe';

describe('ResponsiveHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsiveHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
